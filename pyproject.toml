[build-system]
requires = ["hatchling>=1.25"]
build-backend = "hatchling.build"

[project]
name = "ufcalendar"
version = "0.4.1"
description = "Python client for the UFCalendar Fight API: UFC, PFL, OKTAGON, BKFC and RIZIN events, fight cards, results, per-round stats, fighter careers and UFC rankings history since 2013."
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.9"
authors = [{ name = "UFCalendar", email = "api@ufcalendar.com" }]
keywords = ["ufc", "mma", "api", "ufc-api", "mma-api", "sports-data", "fight-data", "rankings", "bkfc", "pfl", "mcp", "mcp-server", "ufc-mcp", "mma-mcp", "fight-api"]
classifiers = [
  "Development Status :: 4 - Beta",
  "Intended Audience :: Developers",
  "License :: OSI Approved :: MIT License",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3 :: Only",
  "Topic :: Internet :: WWW/HTTP",
  "Topic :: Software Development :: Libraries :: Python Modules",
]
dependencies = ["requests>=2.28"]

[project.urls]
Homepage = "https://www.ufcalendar.com/developers"
Documentation = "https://api.ufcalendar.com/docs"
"UFC API" = "https://www.ufcalendar.com/developers/ufc-api"
"OpenAPI spec" = "https://api.ufcalendar.com/openapi.json"
Source = "https://github.com/UFCalendar/ufcalendar-python"
Issues = "https://github.com/UFCalendar/ufcalendar-python/issues"

[tool.hatch.build.targets.wheel]
packages = ["ufcalendar"]

[tool.hatch.build.targets.sdist]
include = ["ufcalendar", "README.md", "LICENSE", "pyproject.toml", "tests"]
